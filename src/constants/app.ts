/** Tên ứng dụng hiển thị trên UI (sidebar, top bar...). */
export const APP_NAME = "Quản lý rau cải";

/** Key localStorage lưu access token. */
export const ACCESS_TOKEN_KEY = "accessToken";

/** Key localStorage lưu refresh token. */
export const REFRESH_TOKEN_KEY = "refreshToken";
